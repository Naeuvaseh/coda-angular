import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-pizza',
  templateUrl: './pizza.component.html',
  styleUrls: ['./pizza.component.css']
})
export class PizzaComponent implements OnInit {

  constructor() {
    /*
      #1: In /app, create 'containers' directory.

      #2: In /app, create 'components' directory.

      #3: Move PizzaComponent into /containers.

      #4: Create 'topping' component in /components

      #5: Create toppings array variable in ToppingComponent.

      #6: Create topping interface to describe our toppings.

      #7: Update topping array variable.

      #7: In /app, create 'model's directory.

      #7: Move Toppings interface into /models.

      #8: Update imports.
    */
  }

  ngOnInit() {
  
  }

}